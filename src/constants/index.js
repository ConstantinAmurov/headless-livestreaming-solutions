const defaultIpAddress = "localhost";
const defaultRTMPIpAdress = `rtmp://${defaultIpAddress}/live/test`;


module.exports = {
  defaultSceneName,
  defaultWebCamName,
  webCamSourceName,
  defaultIpAddress,
  defaultPort,
  defaultRTMPIpAdress,
};
